#include"main.h"
/**
 * _sqrt_recursion -evaluat ethe sqrt.
 * @n: intiger
 * @i: intiger
 * Return: sqrt
 */
int _sqrt_recursion(int n);
int find_sqrt(int num, int root)
{
	if ((root * root) == num)
		return (root);
}
